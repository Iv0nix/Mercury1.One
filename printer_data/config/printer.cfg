#### Normales EDDY 


[include mainsail.cfg]

# This file contains common pin mappings for the BIGTREETECH Manta M8P V2.0
# To use this config, the firmware should be compiled for the
# STM32H723 with a "128KiB bootloader" "25 MHz crystal"
# and "USB (on PA11/PA12)", "CAN bus (on PD0/PD1)" or Serial (on USART1 PA10/PA9).

# See docs/Config_Reference.md for a description of parameters.

########Hardware########
[include manta-m8p-V2_0.cfg]
[mcu]
serial: /dev/serial/by-id/usb-Klipper_stm32h723xx_35002C001051313234353230-if00


############################ Nachbau Klippain nur Macros ###################

# Lade nur die Klippain-Logik
[include macros/base/*.cfg]
[include macros/helpers/*.cfg] # Falls du globbing nutzt

# Deine angepasste Variablen-Datei
#[include variables.cfg]



[exclude_object]

#-------------------------#
#   Probe Offsets         #
#-------------------------#


[screws_tilt_adjust]
screw1: 70, 66
screw1_name: front left screw
screw2: 378, 66
screw2_name: front right screw
screw3: 378, 337
screw3_name: rear right screw
screw4: 70, 337
screw4_name: rear left screw
horizontal_move_z: 10.
speed: 50.
screw_thread: CW-M4


[mcu eddy]
serial: /dev/serial/by-id/usb-Klipper_rp2040_50445061089B731C-if00
restart_method:command

[temperature_sensor btt_eddy_mcu]
sensor_type: temperature_mcu # Sets the type of sensor for Klipper to read
sensor_mcu: eddy # Sets the MCU of the eddy probe tempereature sensor
min_temp: 10 # Sets the minimum tempereature for eddys tempereature sensor to operate
max_temp: 100 # Sets the maximum tempereature for eddys tempereature sensor to operate

[temperature_probe btt_eddy]
sensor_type: Generic 3950
sensor_pin: eddy:gpio26
horizontal_move_z: 2
#min_temp:
#max_temp:
#   Temperature sensor configuration.
#   See the "extruder" section for the definition of the above
#   parameters.
#smooth_time:
#   A time value (in seconds) over which temperature measurements will
#   be smoothed to reduce the impact of measurement noise. The default
#   is 2.0 seconds.
#gcode_id:
#   See the "heater_generic" section for the definition of this
#   parameter.
#speed:
#   The travel speed [mm/s] for xy moves during calibration.  Default
#   is the speed defined by the probe.
#horizontal_move_z:
#   The z distance [mm] from the bed at which xy moves will occur
#   during calibration. Default is 2mm.
#resting_z:
#   The z distance [mm] from the bed at which the tool will rest
#   to heat the probe coil during calibration.  Default is .4mm
calibration_position: 185, 188, 10
#   The X, Y, Z position where the tool should be moved when
#   probe drift calibration initializes.  This is the location
#   where the first manual probe will occur.  If omitted, the
#   default behavior is not to move the tool prior to the first
#   manual probe.
#calibration_bed_temp:
#   The maximum safe bed temperature (in C) used to heat the probe
#   during probe drift calibration.  When set, the calibration
#   procedure will turn on the bed after the first sample is
#   taken.  When the calibration procedure is complete the bed
#   temperature will be set to zero.  When omitted the default
#   behavior is not to set the bed temperature.
calibration_extruder_temp: 150
#   The extruder temperature (in C) set probe during drift calibration.
#   When this option is supplied the procedure will wait for until the
#   specified temperature is reached before requesting the first manual
#   probe.  When the calibration procedure is complete the extruder
#   temperature will be set to 0.  When omitted the default behavior is
#   not to set the extruder temperature.
extruder_heating_z: 50.
#   The Z location where extruder heating will occur if the
#   "calibration_extruder_temp" option is set.  Its recommended to heat
#   the extruder some distance from the bed to minimize its impact on
#   the probe coil temperature.  The default is 50.
max_validation_temp: 60.
#   The maximum temperature used to validate the calibration.  It is
#   recommended to set this to a value between 100 and 120 for enclosed
#   printers.  The default is 60.

[probe_eddy_current btt_eddy]
sensor_type: ldc1612
descend_z: 0.5
#z_offset: 2
#i2c_address:
i2c_mcu: eddy  # This value is good for the Eddy USB but would need to be adjusted for the Eddy Coil according to the MCU you have used.
i2c_bus: i2c0f # This value is good for the Eddy USB but would need to be adjusted for the Eddy Coil according to the I2C bus you have used.
# Measure the offsets below using the method described here: https://www.klipper3d.org/Probe_Calibrate.html#calibrating-probe-x-and-y-offsets
# For a standard Voron stealthburner X carriage mount there should be no need to change the defaults below.
x_offset: -34.25
y_offset: -14.66

tap_threshold: 120
#   Noise cutoff/stop trigger threshold (in Hz). Specify this value to
#   enable support for "METHOD=tap" probe commands. See Eddy_Probe.md
#   for more information. Larger values make the tap detection less
#   sensitive. That is, larger values make it less likely the toolhead
#   will incorrectly stop early due to noise, while increasing the
#   risk of the toolhead not correctly stopping when it first contacts
#   the bed. If this value is specified then one may override its
#   value at run-time using the "TAP_THRESHOLD" parameter on probe
#   commands. The default is to not enable support for "tap" probing.
tap_z_offset: 0.0
#   The Z height (in mm) of the nozzle relative to the bed at the
#   contact point detected during "tap" probing. Nominally this would
#   be 0.0 to indicate the contact point has zero distance, but one
#   may set this to account for backlash, thermal expansion, a
#   systemic probing bias, or similar. The default is zero.



# This section is only relevant for Eddy USB. Comment it out for Eddy Coil.

#Anpassung der Globalen Mesh Varaiable 
#[gcode_macro BED_MESH_CALIBRATE]
#rename_existing: _BED_MESH_CALIBRATE_BASE
#gcode:
#    # Hier rufen wir den Original-Befehl mit deinen Eddy-Parametern auf
#    _BED_MESH_CALIBRATE_BASE METHOD=scan SCAN_MODE=rapid {rawparams}

#[gcode_macro BED_MESH_CALIBRATE_FAST]
## Dieser Befehl sagt Klipper: "Nimm das bestehende Makro und nenne es um"
#rename_existing: _BED_MESH_CALIBRATE_FAST_JSCHUH
#gcode:
#    # 1. Sicherstellen, dass wir in der Bettmitte sind, bevor wir absenken
#    # Das verhindert den Trigger-Fehler am Rand oder über Klammern
#    {% if "xyz" not in printer.toolhead.homed_axes %}
#        G28
#    {% endif %}
#
#    G90
#    G1 X190 Y190 Z10 F6000 # Mitte bei 380er Bett
#    
#    # 2. Kurze Pause für den Sensor
#    G4 P500
#    
#    # 3. Den Eddy-Scan mit deinen Parametern aufrufen
#    # Wir ignorieren das jschuh-Makro hier komplett und gehen direkt auf die Basis
#    _BED_MESH_CALIBRATE_BASE METHOD=scan SCAN_MODE=rapid HORIZONTAL_MOVE_Z=2 {rawparams}
#    
#    # 4. Wieder auf Sicherheitsabstand
#    G1 Z10 F3000


[bed_mesh]
horizontal_move_z: 2
speed: 200
# For the mesh dimensions below, the coordinates need to be reachable by the center of the probe. To calculate coordinates that will work, use the formula below:
# mesh x min = position_min_x + greater_of (15mm or x_offset) <--- in this term, only consider the x offset if it is positive, ignore if negative.
# mesh y min = position_min_y + greater_of (15mm or y_offset) <--- in this term, only consider the y offset if it is positive, ignore if negative.
# mesh x max = position_max_x - greater_of (15mm or |x_offset|) <--- in this term, only consider the x offset if it is negative, ignore if positive.
# mesh y max = position_max_y - greater_of (15mm or |y_offset|) <--- in this term, only consider the y offset if it is negative, ignore if positive.
# Example: Consider that you have a 300 x 300 bed with the max x and y positions being 300 and the min being 0. Your probe offsets are -20 for X and 10 for Y
# For mesh x min we ignore the x offset term because it is negative. Therefore mesh x min = 15
# For mesh y min we do not ignore the y offset term because it is positive but it is less than 15 so we use 15. Therefore mesh y min = 15
# For mesh x max we do not ignore the x offset term because it is negative. It is also greater than 15. Therefore mesh x max = 280
# For mesh y max we ignore the y offset term because it is positive but it is less than 15 so we use 15. Therefore mesh y max = 285
# The final result would be mesh_min: 15, 15 mesh_max: 280, 285
#mesh_min: 10, 10  # modify these according to the above guide. If the probe cannot reach then you will get a klipper error when trying to scan a bed mesh.
#mesh_max: 360, 360 # modify these according to the above guide. If the probe cannot reach then you will get a klipper error when trying to scan a bed mesh.
mesh_min: 30, 20
mesh_max: 340, 360
probe_count: 9, 9
algorithm: bicubic
#scan_overshoot: 8  #uncomment this section if you still have room left over on the X axis for some scan overshoot to product smoother movements and more accurate scanning. Uncommenting this should be fine if you are using a standard voron mount.

# Uncomment this if you are using Eddy as the probe AND the homing endstop
[safe_z_home]
home_xy_position: 185, 185 # Choose an X,Y position that is in the center of your bed. For a 300x300 machine that will be 150, 150. Use the same principle to calculate your bed center.
z_hop: 10
z_hop_speed: 25
speed: 200

###############################Macros and related################################
#This secton contains macros and related config sections. Some should be used, others are optional. Read the comment above each one to find out whether or not to uncomment it for your installation.


# Uncomment this if you are using Eddy as the probe AND the homing endstop AND would like to use the beta z-offset control
[save_variables]
filename: ~/printer_data/config/variables.cfg



# Uncomment this if you are using Eddy as the probe AND the homing endstop
[force_move]
enable_force_move: True # Allows a user to move the z axis down if they have no other means of homing Z and need to calibrate the Eddy.



# Uncomment this if you are using Eddy as the probe AND the homing endstop AND would like to use the beta z-offset control
[delayed_gcode RESTORE_PROBE_OFFSET]
initial_duration: 1.
gcode:
  {% set svv = printer.save_variables.variables %}
  {% if not printer["gcode_macro SET_GCODE_OFFSET"].restored %}
    SET_GCODE_VARIABLE MACRO=SET_GCODE_OFFSET VARIABLE=runtime_offset VALUE={ svv.nvm_offset|default(0) }
    SET_GCODE_VARIABLE MACRO=SET_GCODE_OFFSET VARIABLE=restored VALUE=True
  {% endif %}



# Uncomment this if you are using Eddy as the probe AND the homing endstop
# Take note of the lines that should only be used if you have a KNOMI installed.
[gcode_macro G28]
rename_existing: G28.1
gcode:
  #SET_GCODE_VARIABLE MACRO=_KNOMI_STATUS VARIABLE=homing VALUE=True # Uncomment this if using a KNOMI and then remove the G28 macro from the KNOMI.cfg
  G28.1 {rawparams}
  {% if not rawparams or (rawparams and 'Z' in rawparams) %}
    PROBE
    SET_Z_FROM_PROBE
  {% endif %}
  #SET_GCODE_VARIABLE MACRO=_KNOMI_STATUS VARIABLE=homing VALUE=False # Uncomment this if using a KNOMI and then remove the G28 macro from the KNOMI.cfg



# Uncomment this if you are using Eddy as the probe AND the homing endstop
#[gcode_macro SET_Z_FROM_PROBE]
#gcode:
#    {% set cf = printer.configfile.settings %}
#    SET_GCODE_OFFSET_ORIG Z={printer.probe.last_z_result - cf['probe_eddy_current btt_eddy'].z_offset + printer["gcode_macro SET_GCODE_OFFSET"].runtime_offset}
#    G90
#    G1 Z{cf.safe_z_home.z_hop}

######new######

[gcode_macro _RELOAD_Z_OFFSET_FROM_PROBE]
gcode:
  {% set Z = printer.toolhead.position.z %}
  SET_KINEMATIC_POSITION Z={Z - printer.probe.last_probe_position.z}

[gcode_macro SET_Z_FROM_PROBE]
gcode:
  {% set METHOD = params.METHOD | default("automatic") %}
  PROBE METHOD={METHOD}
  _RELOAD_Z_OFFSET_FROM_PROBE
  G0 Z5
#####new########

# Uncomment this if you are using Eddy as the probe AND the homing endstop AND would like to use the beta z-offset control
[gcode_macro Z_OFFSET_APPLY_PROBE]
rename_existing: Z_OFFSET_APPLY_PROBE_ORIG
gcode:
  SAVE_VARIABLE VARIABLE=nvm_offset VALUE={ printer["gcode_macro SET_GCODE_OFFSET"].runtime_offset }



# Uncomment the lines in this macro if you are using Eddy as the probe AND the homing endstop AND would like to use the beta z-offset control
[gcode_macro SET_GCODE_OFFSET]
rename_existing: SET_GCODE_OFFSET_ORIG
variable_restored: False  # Mark whether the var has been restored from NVM
variable_runtime_offset: 0
gcode:
  {% if params.Z_ADJUST %}
    SET_GCODE_VARIABLE MACRO=SET_GCODE_OFFSET VARIABLE=runtime_offset VALUE={ printer["gcode_macro SET_GCODE_OFFSET"].runtime_offset + params.Z_ADJUST|float }
  {% endif %}
  {% if params.Z %} 
    {% set paramList = rawparams.split() %}
    {% for i in range(paramList|length) %}
      {% if paramList[i]=="Z=0" %}
        {% set temp=paramList.pop(i) %}
        {% set temp="Z_ADJUST=" + (-printer["gcode_macro SET_GCODE_OFFSET"].runtime_offset)|string %}
        {% if paramList.append(temp) %}{% endif %}
      {% endif %}
    {% endfor %}
    {% set rawparams=paramList|join(' ') %}
    SET_GCODE_VARIABLE MACRO=SET_GCODE_OFFSET VARIABLE=runtime_offset VALUE=0
  {% endif %}
  SET_GCODE_OFFSET_ORIG { rawparams }



# This macro automates a lot of the frequency mapping process and simplifies the steps significantly.
[gcode_macro PROBE_EDDY_CURRENT_CALIBRATE_AUTO]
gcode:
  BED_MESH_CLEAR
  G28 X Y
  G90 # Abs positioning
  G1 X{ printer.toolhead.axis_maximum.x/2 } Y{ printer.toolhead.axis_maximum.y/2 } F6000
  {% if 'z' not in printer.toolhead.homed_axes %}
    SET_KINEMATIC_POSITION Z={ printer.toolhead.axis_maximum.z-1 } # Allows the user to work it down until it touches.
  {% endif %}
  PROBE_EDDY_CURRENT_CALIBRATE {rawparams}



#This macro is optional but useful if you want to run a rapid scan before each print. Simply uncomment it and add BED_MESH_SCAN to your print start code.
#[gcode_macro BED_MESH_CALIBRATE]
#rename_existing: BTT_BED_MESH_CALIBRATE
#gcode:
#  SET_GCODE_VARIABLE MACRO=_KNOMI_STATUS VARIABLE=probing VALUE=True #Only uncomment this line if using a KNOMI and then remove the BED_MESH_CALIBRATE macro from KNOMI.cfg
#  BTT_BED_MESH_CALIBRATE METHOD=rapid_scan
#  SET_GCODE_VARIABLE MACRO=_KNOMI_STATUS VARIABLE=probing VALUE=False #Only uncomment this line if using a KNOMI and then remove the BED_MESH_CALIBRATE macro from KNOMI.cfg

#*# <---------------------- SAVE_CONFIG ---------------------->
#*# DO NOT EDIT THIS BLOCK OR BELOW. The contents are auto-generated.
#*#
#*# [probe_eddy_current btt_eddy]
#*# reg_drive_current = 15
#*# calibrate =
#*# 	0.050271:3298492.491,0.090488:3296726.975,0.129448:3295147.587,
#*# 	0.169665:3293505.095,0.209882:3292005.175,0.250099:3290484.077,
#*# 	0.290316:3288974.429,0.330533:3287429.102,0.369493:3285955.567,
#*# 	0.409710:3284469.888,0.449926:3283014.876,0.490143:3281654.514,
#*# 	0.530360:3280309.964,0.570577:3279063.545,0.609537:3277794.657,
#*# 	0.649754:3276489.901,0.689971:3275190.357,0.730188:3273904.499,
#*# 	0.770405:3272551.782,0.810622:3271278.627,0.849582:3270068.285,
#*# 	0.889799:3268836.415,0.930015:3267593.733,0.970232:3266370.223,
#*# 	1.010449:3265158.463,1.049409:3264094.534,1.089626:3263024.061,
#*# 	1.129843:3261952.375,1.170060:3260940.685,1.210277:3259981.363,
#*# 	1.250494:3259029.669,1.289454:3258119.738,1.329671:3257190.796,
#*# 	1.369888:3256276.918,1.410104:3255345.422,1.450321:3254417.850,
#*# 	1.490538:3253463.305,1.529498:3252528.602,1.569715:3251650.972,
#*# 	1.609932:3250769.874,1.650149:3249897.752,1.690366:3249002.616,
#*# 	1.730583:3248128.592,1.769543:3247234.815,1.809760:3246356.131,
#*# 	1.849977:3245476.388,1.890194:3244603.665,1.930410:3243753.709,
#*# 	1.970627:3242948.799,2.009587:3242155.885,2.049804:3241388.608,
#*# 	2.090021:3240660.564,2.130238:3239972.795,2.170455:3239246.074,
#*# 	2.209415:3238597.698,2.249632:3237975.791,2.289849:3237370.767,
#*# 	2.330066:3236745.737,2.370283:3236117.163,2.410499:3235450.294,
#*# 	2.449460:3234853.038,2.489676:3234259.594,2.529893:3233679.407,
#*# 	2.570110:3233063.035,2.610327:3232440.158,2.650544:3231800.265,
#*# 	2.689504:3231194.583,2.729721:3230602.838,2.769938:3230013.209,
#*# 	2.810155:3229418.975,2.850372:3228879.969,2.890589:3228321.701,
#*# 	2.929549:3227792.666,2.969766:3227279.284,3.009982:3226811.964,
#*# 	3.050199:3226351.823,3.090416:3225906.579,3.129376:3225482.147,
#*# 	3.169593:3225057.464,3.209810:3224618.583,3.250027:3224202.339,
#*# 	3.290244:3223738.757,3.330461:3223312.658,3.369421:3222916.857,
#*# 	3.409638:3222549.087,3.449855:3222206.182,3.490071:3221816.079,
#*# 	3.530288:3221440.734,3.570505:3221075.703,3.609465:3220715.000,
#*# 	3.649682:3220344.651,3.689899:3219946.128,3.730116:3219542.066,
#*# 	3.770333:3219132.446,3.810550:3218763.070,3.849510:3218417.620,
#*# 	3.889727:3218110.317,3.929944:3217795.154,3.970160:3217500.973,
#*# 	4.010377:3217212.571,4.050594:3216935.223
#*#
#*# [temperature_probe btt_eddy]
#*# calibration_temp = 36.104866
#*#
#*# [bed_mesh default]
#*# version = 1
#*# points =
#*# 	0.276529, 0.185645, 0.158013, 0.125995, 0.057063, -0.009947, -0.077423, -0.113948, -0.165765
#*# 	0.236973, 0.244028, 0.179897, 0.122163, 0.043380, -0.038309, -0.100794, -0.133476, -0.189117
#*# 	0.257721, 0.241194, 0.160952, 0.084530, -0.009976, -0.092696, -0.160374, -0.204285, -0.251843
#*# 	0.234334, 0.197821, 0.107731, 0.034940, -0.062836, -0.145085, -0.190626, -0.234397, -0.282129
#*# 	0.253689, 0.209134, 0.095076, 0.009773, -0.097034, -0.182098, -0.245728, -0.277387, -0.322535
#*# 	0.268328, 0.222557, 0.105416, 0.006336, -0.109964, -0.204427, -0.264416, -0.292091, -0.327520
#*# 	0.284011, 0.222228, 0.103257, -0.001796, -0.133826, -0.232689, -0.291553, -0.321936, -0.354998
#*# 	0.255283, 0.204734, 0.081007, -0.033717, -0.160939, -0.261334, -0.320448, -0.347020, -0.368197
#*# 	0.311852, 0.245785, 0.104684, 0.001347, -0.133950, -0.243753, -0.284676, -0.309076, -0.344241
#*# x_count = 9
#*# y_count = 9
#*# mesh_x_pps = 2
#*# mesh_y_pps = 2
#*# algo = bicubic
#*# tension = 0.2
#*# min_x = 15.0
#*# max_x = 339.96
#*# min_y = 20.0
#*# max_y = 360.0
